# tests/

All tests are organized into two consolidated projects.

## Subdirectories

| Directory | What | When to read |
| --------- | ---- | ------------ |
| `Emit.UnitTests/` | All unit tests (458 tests) with subfolders mirroring src/ structure | Write or debug unit tests |
| `Emit.IntegrationTests/` | All integration tests with shared base classes and provider-specific tests | Write or debug integration tests |
