<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>Emit.Persistence.MongoDB</RootNamespace>
    <AssemblyName>Emit.Persistence.MongoDB</AssemblyName>
    <PackageId>Emit.Persistence.MongoDB</PackageId>
    <Description>MongoDB persistence provider for Emit. Stores outbox entries in MongoDB with support for sharding and atomic sequence generation.</Description>
    <PackageTags>outbox;transactional;mongodb;messaging;reliability</PackageTags>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Emit\Emit.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="FluentValidation" />
    <PackageReference Include="MongoDB.Driver" />
    <PackageReference Include="Transactional.MongoDB" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Emit.Tests" />
    <InternalsVisibleTo Include="Emit.IntegrationTests" />
  </ItemGroup>

</Project>
