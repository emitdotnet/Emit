# Emit.Kafka.AvroSerializer

Avro serialization support for Emit's Kafka producers, backed by Confluent Schema Registry. For teams who have embraced Avro and would rather not write the wiring themselves.

Requires `Emit.Kafka`.
