# Emit.OpenTelemetry

Plugs Emit's built-in metrics into your OpenTelemetry pipeline. One registration call and outbox depth, delivery latency, and processing outcomes start flowing into wherever your telemetry goes — because knowing something is wrong before your users do is always the goal.

You can also attach static tags (environment, region, deployment name, whatever you track) to all Emit metrics in one place.
