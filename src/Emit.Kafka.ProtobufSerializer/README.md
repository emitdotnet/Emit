# Emit.Kafka.ProtobufSerializer

Protobuf serialization support for Emit's Kafka producers, backed by Confluent Schema Registry. For teams who like their messages compact, strongly typed, and forward-compatible.

Requires `Emit.Kafka`.
