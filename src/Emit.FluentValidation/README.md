# Emit.FluentValidation

FluentValidation integration for Emit. Bridges `IValidator<T>` validators into the Emit consumer validation pipeline via `ValidateWithFluentValidation()` on any consumer group builder.
