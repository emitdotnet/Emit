# Emit.Kafka.JsonSerializer

JSON Schema serialization support for Emit's Kafka producers, backed by Confluent Schema Registry. Schema-validated messages without the plumbing.

Requires `Emit.Kafka`.
